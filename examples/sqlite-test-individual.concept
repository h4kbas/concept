# Test individual SQLite commands
sqlite open test.db
sqlite query CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, email TEXT)
sqlite query INSERT INTO users (name, email) VALUES ('Alice', 'alice@example.com')
sqlite query SELECT * FROM users
sqlite query SELECT COUNT(*) as count FROM users
sqlite close
